# Contracts

Purpose: catalog public, persisted, serialized, operational, and cross-context contracts that future coding agents must not reshape casually.

## Contract Catalog

| Contract | Producer | Known Consumers | Schema / Interface | Compatibility Rule | Test Coverage | Risk If Changed | Evidence | Discovery Status |
|---|---|---|---|---|---|---|---|---|
| CLI command surface | CLI / Operator Surface | Human operators, docs, tests, generated CI, shell scripts | `src/ces/cli/__init__.py`; command modules under `src/ces/cli/` | Additive commands/options are safest; renames/removals are source-breaking and docs-breaking. | `tests/unit/test_cli/`; `tests/integration/test_freshcart_e2e.py`; docs tests | Source-breaking / behaviorally risky | Fact | External shell users unknown. |
| Global `--json` outputs | CLI / Output helpers | Human operators, tests, potential automation | `src/ces/cli/_output.py`; command-specific JSON branches | Field renames/removals are wire-breaking if automation consumes them. | CLI JSON tests for status, audit, brownfield, manifest, review, triage, approve, etc. | Wire-breaking | Fact | Stable machine-readable field set is not fully declared. |
| Console entry point | Packaging / CLI | Installed users, `uv tool`, `pipx`, release smoke | `pyproject.toml` `[project.scripts] ces = "ces.cli:app"` | Changing entry point is source-breaking. | `tests/unit/test_docs/test_release_packaging.py`; publish workflow wheel smoke | Source-breaking | Fact | Complete external install base unknown. |
| Python package exports | Package modules | In-repo imports, tests, possible library users | package `__init__.py` files, especially `src/ces/control/models/__init__.py` | Removing or moving exports is source-breaking; preserve import shims where practical. | `tests/unit/test_execution/test_public_exports.py`; model tests | Source-breaking | Fact | External library consumers unknown. |
| `CES_*` environment variables | Shared Config / CLI Factory / Runtime / Observability | Operators, CI, tests, runtime detection | `src/ces/shared/config.py`; `.env.example`; `src/ces/cli/_factory.py`; `src/ces/execution/runtimes/registry.py`; `src/ces/observability/otel.py` | Additive env vars are compatible; changing defaults/meaning is operationally breaking. | config, doctor, runtime, observability tests | Operationally breaking | Fact | Some settings (`model_roster`, `default_model_id`) are less documented than runtime vars. |
| Local `.ces/` filesystem layout | Local Store / CLI | Operators, backup/restore, builder/report/status commands | `.ces/state.db`, `.ces/keys/`, `.ces/artifacts/`, `.ces/exports/`, `.ces/baseline/`, `.ces/brownfield/scan.json` | Moving files or changing permissions is operationally breaking. | local-store, CLI factory, scan/baseline/builder tests; docs tests | Operationally / persistence breaking | Fact | Complete generated-subdirectory list should stay current. |
| Local SQLite schema | Local Store | Repository adapters, services, existing user DBs, CLI reports/status | DDL and in-app migrations in `src/ces/local_store/store.py`; records in `src/ces/local_store/records.py` | Add nullable columns/tables; never drop/rename without migration and old-shape regression tests. | `tests/unit/test_local_store.py`; builder/CLI integration tests | Persistence-breaking | Fact | No explicit schema version ledger found. |
| Audit ledger append-only hash chain | Control / Local Store | Operators, audit CLI, merge/review/emergency flows, tests | `audit_entries` table, triggers, `AuditLedgerService`, `AuditEntry` model, `EventType` enum | Updating/deleting entries or changing hash inputs is persistence-breaking. | `tests/integration/test_audit_chain.py`; audit/local-store tests | Persistence-breaking / integrity risk | Fact | Public integrity verifier behavior should be treated as stable if used in docs. |
| Task manifest schema and signatures | Control Plane | Builder, expert CLI, execution, review, merge, spec, local store | `TaskManifest`, `ManifestDependency`, `RiskTier`, `BehaviorConfidence`, `ChangeClass`, `WorkflowState` | Optional additive fields are safest; required fields, enum value changes, signing/hash changes need migration. | manifest model/service tests; CLI workflow tests; integration lifecycle tests | Source-, wire-, and persistence-breaking | Fact | Manifest `version` migration semantics are unclear. |
| Truth artifact schemas | Control Plane | Manifest dependencies, invalidation, import/export, tests | `TruthArtifact` union and individual models in `src/ces/control/models/` | `schema_type` and required field changes are wire/source-breaking. | model tests; invalidation tests | Source/wire-breaking | Fact | Current persistence beyond manifests is not fully inventoried. |
| Evidence packet and persisted evidence dict | Harness / Control / Local Store | Review, triage, approval, report builder, status/explain, existing DBs | `EvidencePacket`; `LocalProjectStore.save_evidence`; `evidence_packets.content` | Keep model and stored dict readable together; removing fields is wire/persistence risky. | evidence model, review, triage, approve, builder report tests | Wire- and persistence-breaking | Fact | Whether stored dict is official JSON contract is open. |
| Review finding schema and persistence | Harness / Local Store | Review pipeline, report builder, status/explain, existing DBs | `ReviewFinding`; `review_findings` table | Do not reintroduce uniqueness on reviewer-provided `finding_id`; add columns compatibly. | review finding model, local-store, review router tests | Persistence-breaking / report-breaking | Fact | Stable category taxonomy is unclear. |
| Runtime adapter protocol | Execution | Runtime registry, agent runner, builder flow, tests, potential external adapters | `AgentRuntimeProtocol`; `AgentRuntimeResult` | Signature changes are source-breaking; semantic changes affect evidence. | runtime adapter and agent runner tests | Source-breaking / behaviorally risky | Fact | External adapter consumers unknown. |
| Completion claim fenced block | Execution / Harness | Agent output, parser, verifier, repair loop, review evidence | Fenced block tagged `ces:completion`; JSON `CompletionClaim` | Parser/schema changes can silently lose evidence; version if shape changes. | completion parser/model/verifier/execute tests | Wire-breaking / behaviorally risky | Fact | External-agent documentation is incomplete. |
| Brownfield OLB contract | Brownfield / Local Store | Builder, `ces brownfield`, PRL promotion, local DBs | `LegacyBehaviorRecord`, `ObservedLegacyBehavior`, `legacy_behaviors` table, OLB IDs, `LegacyDisposition` | Disposition vocabulary or OLB ID shape changes are source/wire/persistence breaking. | brownfield service/CLI tests; builder brownfield tests | Persistence- and source-breaking | Fact | Docs vocabulary mismatch needs owner decision. |
| PRL promotion contract | Brownfield / Control | Brownfield service, PRL model, local store, audit | `promote_to_prl`, `PRLItem`, `prl_items` table, source OLB back-reference | Preserve copy-on-promote and original register entry. | brownfield service tests | Behaviorally / persistence risky | Fact | PRL lifecycle beyond promotion is not fully mapped. |
| Spec Markdown format | Spec Authoring | `ces spec`, `ces build --from-spec`, fixtures, property tests | YAML frontmatter, required sections, story fields, `src/ces/control/spec/templates/default.yaml` | Required section/field changes are wire-breaking for existing specs. | spec CLI, integration, property tests | Wire-breaking | Fact | Public stability of templates and format is open. |
| Generated CI templates | CLI / Packaging | User repos running `ces setup-ci`, GitHub/GitLab CI | `src/ces/cli/templates/ci/github.yml`; `gitlab-ci.yml`; `src/ces/cli/setup_ci_cmd.py` | Tightening behavior can break user CI; version/deprecate material changes. | `tests/unit/test_cli/test_setup_ci_cmd.py`; docs CI tests | Operationally breaking | Fact | Template compatibility policy is open. |
| Starter manifest templates | CLI / Control | `ces init --template`, human-edited starter manifests | `src/ces/cli/templates/manifests/*.yaml` | Field names should match `TaskManifest` or be intentionally transitional. | init template tests | Source/user-workflow breaking | Fact | Current starter values are human-editable examples, not strict manifests. |
| Local-first project config | CLI / Factory | `get_services`, operators, tests | `.ces/config.yaml` keys such as `project_id`, `project_name`, `preferred_runtime`, `execution_mode` | `execution_mode: server` is rejected; key renames are operationally breaking. | factory, execute, local-first tests | Operationally breaking | Fact | Full config schema is not centralized as a Pydantic model. |
| Optional compatibility migrations | Compatibility Tests | CI/dev compatibility suite only | root `alembic.ini`; `tests/integration/_compat/alembic/versions/`; `compat-tests` extra | Treating these as production migrations is a product-contract error. | `tests/integration/_compat/`; integration fixtures | Operationally breaking if misapplied | Fact | Retention/deprecation plan is open. |
| Publish and release workflow | Packaging / CI | Maintainers, PyPI release process | `.github/workflows/publish.yml`; `uv build`; `twine check`; installed wheel smoke | Removing pre-publish tests or CLI smoke weakens release gate. | docs release packaging tests; workflow tests | Release-quality risk | Fact | None. |

## Compatibility Classifications

- Additive compatible: new CLI subcommands, optional fields with defaults, new optional env vars, new SQLite tables or nullable columns.
- Behaviorally risky: classification logic, review routing, merge gate rules, sensor selection, runtime resolution order, `CES_DEMO_MODE` semantics.
- Source-breaking: CLI command/flag removals, package export removals, runtime protocol signature changes.
- Wire-breaking: JSON output field changes, Pydantic serialized field changes, spec Markdown required fields, completion claim schema.
- Persistence-breaking: SQLite table/column drops or renames, audit hash input changes, enum value changes stored in DB.
- Operationally breaking: default runtime/dependency changes, generated CI gate changes, `.ces/` layout changes, making Postgres mandatory.

## Consumer Discovery Notes

| Contract | In-Repo Consumers Found | Complete? | Notes |
|---|---|---|---|
| CLI surface | Docs, unit/integration tests, generated CI templates, scripts | No | External shell users unknown. Preserve compatibility. |
| Local SQLite schema | Repository adapters, CLI status/report, control/harness/brownfield services, tests | Mostly | Existing user DBs outside repo cannot be enumerated. |
| Manifest schema | Builder, CLI commands, execution, review, merge, spec, local store | Mostly | External serialized manifest use unknown. |
| Evidence dict | Builder report/status/review/triage paths and tests | No | Need owner decision on stable JSON shape. |
| Runtime protocol | In-repo Codex/Claude adapters and tests | Yes for in-repo | External adapters unknown. |
| Spec file format | CLI spec commands, fixtures, property tests, build-from-spec tests | Mostly | Public stability needs explicit decision. |
| Generated CI templates | Setup-ci command tests and generated YAML | Mostly | User repos generated from older versions unknown. |

## Rules For Future Contract Changes

- Do not rename public command names, flags, JSON fields, enum values, table names, Pydantic serialized fields, or env vars without migration/deprecation notes and tests.
- Keep `README.md`, `.env.example`, CLI help, docs tests, and command behavior aligned when changing operator contracts.
- Add local-store migrations in `LocalProjectStore.initialize()` with old-shape regression tests; do not rely on compatibility Alembic for product SQLite state.
- Do not change audit ledger hash inputs or manifest signing payloads without an explicit backward-compatibility story.
- Keep optional compatibility surfaces (`tests/integration/_compat/`, root `alembic.ini`) out of the supported local-first runtime path.
