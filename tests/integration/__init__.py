"""Integration tests for CES.

Most tests exercise the supported local builder-first workflow. A smaller
compatibility subset still uses Docker-backed PostgreSQL containers to verify
legacy repository and migration code paths.
"""
